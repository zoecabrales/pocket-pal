import { styled } from "styled-components";
import { InnerLayout } from "../../styles/layouts";

function Options() {
  return (
    <OptionStyled>
      <InnerLayout>Options</InnerLayout>
    </OptionStyled>
  );
}

const OptionStyled = styled.div``;

export default Options;
